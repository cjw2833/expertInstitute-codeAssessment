import { CurrencySearchFilterPipe } from './currency-search-filter.pipe';

describe('CurrencySearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
